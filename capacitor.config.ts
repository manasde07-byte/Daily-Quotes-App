import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elite.bengalistatus',
  appName: 'Elite Bengali Status',
  webDir: 'dist'
};

export default config;
